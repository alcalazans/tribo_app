import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:tribo/app/modules/contrato/contrato_controller.dart';
import 'package:tribo/app/modules/contrato/contrato_module.dart';

void main() {
  initModule(ContratoModule());
  // ContratoController contrato;
  //
  setUp(() {
    //     contrato = ContratoModule.to.get<ContratoController>();
  });

  group('ContratoController Test', () {
    //   test("First Test", () {
    //     expect(contrato, isInstanceOf<ContratoController>());
    //   });

    //   test("Set Value", () {
    //     expect(contrato.value, equals(0));
    //     contrato.increment();
    //     expect(contrato.value, equals(1));
    //   });
  });
}
