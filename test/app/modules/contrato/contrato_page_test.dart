import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:tribo/app/modules/contrato/contrato_page.dart';

main() {
  testWidgets('ContratoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContratoPage(title: 'Contrato')));
    //  final titleFinder = find.text('Contrato');
    //  expect(titleFinder, findsOneWidget);
  });
}
